{{ ansible_managed | comment }}

[Unit]
Description={{ item.value['random'] }} FQDN update service
After=multi-user.target

[Service]
Type=simple
Restart=always
ExecStart=/usr/bin/python3 {{ ipset_config_path }}{{ item.value['random'] }}.py

[Install]
WantedBy=multi-user.target